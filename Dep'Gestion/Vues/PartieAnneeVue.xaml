<Page
    x:Class="Dep_Gestion.Vues.PartieAnneeVue"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dep_Gestion.Vues"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Width="1500" Height="1000">

        <TextBlock 
            FontSize="40"
                   Margin="380,42,861,886"
                   FontFamily="Segoe UI">Partie Année</TextBlock>
        <TextBlock x:Name="SubTitle"
                   HorizontalAlignment="Right"
                   FontSize="36"
                   Margin="20" />
        <TextBlock HorizontalAlignment="Left" Margin="393,104,0,0" Text="{x:Bind Path=tbmodel.Text, Mode=OneWay}" TextWrapping="Wrap" VerticalAlignment="Top" Width="185"/>
        <Button x:Name="btnPartAnnee" Content="{x:Bind Path=bmmodel.Content, Mode=OneWay}" HorizontalAlignment="Left" Margin="434,190,0,0" VerticalAlignment="Top" Click="btnPartAnnee_Click"/>
        <TextBox x:Name="txtbox" HorizontalAlignment="Left" Margin="380,143,0,0" Text="{x:Bind Path=tboxmodel.Text, Mode=OneWay}" VerticalAlignment="Top" Width="165"/>
    </Grid>
</Page>